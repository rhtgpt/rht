we are changing the partitioning method (RANGE+INTERVAL → LIST on a virtual column), 
and adding a virtual column, and 
per‑partition LOB placement. 
Oracle doesn’t support converting RANGE/INTERVAL → LIST via ALTER.
