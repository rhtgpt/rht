Regarding point #3 on creating a catch-up script to select all new rows and sync them to the new table, I would like to request some clarification.

Since I do not have prior experience in implementing such scripts, could you please provide more details on:

The exact criteria we should use to identify “new rows” (e.g., based on AUDIT_CREATE_DATE, last processed timestamp, or another column).

Whether this catch-up process should be a one-time batch load after the initial migration or a recurring/scheduled job.

If you have a sample script or existing approach that I can follow as a reference.
