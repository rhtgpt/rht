 1 * newOrderService.applyRules(_, _, _, _, _, _) >> { args ->
        // Capture the rewardEvents argument if it's passed by reference (list)
        capturedRewardEvents = []
        capturedRewardEvents.add(expectedReward)
        return capturedRewardEvents
    }

    result.size() == 1
    result[0] == expectedReward
